// const sqlite = require("sqlite3").verbose();

// const connectDB = () => {
//   const db = new sqlite.Database("dev.db", sqlite.OPEN_READWRITE, (err) => {
//     if (err) return console.log(err);
//   });

//   db.run("CREATE TABLE lorem (info TEXT)");

//   db.close();
// };

// // export default connectDB;
